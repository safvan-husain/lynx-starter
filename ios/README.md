# iOS Host App

This folder is reserved for the iOS host application.

Next step:

1. Create or move your iOS (Xcode) project into this folder.
2. Register the Lynx native modules (for example `NativeImagePickerModule`).
